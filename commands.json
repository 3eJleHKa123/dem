{
  "hotkey": "F12",
  "note": "В текущей версии данные зашиты в AHK. Этот JSON оставлен как удобный источник для будущей версии.",
  "pages": [
    {
      "title": "Модуль 1: сеть",
      "items": [
        "hostnamectl set-hostname hq-srv.au-team.irpo",
        "nmcli con mod <conn> ipv4.addresses <ip>/<mask> ipv4.gateway <gw>",
        "SSH: Port 2012, AllowUsers sshuser, MaxAuthTries 2",
        "iptables -t nat -A POSTROUTING -o <wan> -j MASQUERADE"
      ]
    },
    {
      "title": "Модуль 2: службы",
      "items": [
        "samba-tool domain provision --realm=AU-TEAM.IRPO",
        "mdadm --create /dev/md2 --level=5 --raid-devices=3 /dev/sd[b-d]",
        "ansible all -m ping -i /etc/ansible/hosts",
        "docker compose up -d"
      ]
    }
  ]
}
